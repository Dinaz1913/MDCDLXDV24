<?php

$numbers = [20, 30, 25, 35, -16, 60, -100];
$Average = array_filter($numbers);
if(count($numbers)) {
    echo "Average number:" . $average = array_sum($numbers)/count($numbers) . "\n";
}
//todo calculate an average value of the numbers